# Host-side development. Nothing here is installed on the device -- the device libraries
# (adafruit_hid, neopixel, adafruit_pixelbuf) are installed onto the board by circup.
-r requirements-test.txt

circup==2.1.0
